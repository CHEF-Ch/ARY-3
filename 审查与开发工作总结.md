# ARY 审查与开发工作总结

日期：2026-06-16 ~ 2026-06-20

---

## 一、已完成工作

### 阶段 1：文档基线审查（6/16）

- **PRD-TEMP-1 复审**：全文档搜索 7 个旧口径关键词，确认在 PRD/领域/IA/权限/QA/OPS/CA 中全部清零；4 条新口径（CA 不作为资格硬门禁、评审前风险提示、RaceProject 幂等生成、参赛中动态新增 CAConnection）跨文档一致落地
- **UX-1 高保真原型审计**：10 页面 × 3 维度（IA 合规/设计护栏/导航状态）审查，发现 23 项问题，修复 11 项关键问题（currentRaceId 污染、私有作品暴露、Cooperation 缺失 IA 内容、导航断头等）
- **文档更新**：`registration-ca-rules-alignment.taskbook.md` v0.2→v0.3 关闭，`PLAN.md` 和 `STATUS.md` 同步更新

### 阶段 2：架构设计（6/16-17）

- **DEV-1 架构设计**：输出 `docs/ary-dev-1-data-model.md`
  - 6 个限界上下文（Identity / Race Management / Portfolio / Report Generation / Projection / Communication）
  - 14 张表结构（字段、类型、约束、不变量引用）
  - 13 组接口鉴权规则（资源 × 动作 × 角色 × 作用域）
  - 23/23 领域不变量覆盖核查
- **模块化研发分工**：输出 `待办与分工文档.md`（后移至根目录），6 模块 × 5 人 × 6 天施工计划
- **SKILL.md 工作流**：首页整改经验固化为 7 步可复用流程

### 阶段 3：项目骨架搭建（6/17）

- **服务端骨架**：Express + TypeScript + JSON 文件存储，零原生依赖
- **鉴权中间件**：`authorize()` + `requireLogin` + `requireAdmin` + `reqUser()`，13 种资源 × 动作的作用域矩阵
- **身份模块（identity）**：注册/登录（用户名密码 + GitHub OAuth）、Admin Console、主办方审核队列
- **沟通模块（communication）**：公告 CRUD、骑手档案跨表聚合
- **前端骨架**：Vite + React + React Router，10 页面脚手架 + Console 子页面拆分
- **开发指南**：`开发指南.md`，含目录结构、后端约定、工作流程、每人交付物、测试方案

### 阶段 4：集成审查与测试（6/19-20）

- **五分支审查**：A(identity+communication)、B(race-mgmt)、C(portfolio)、D(projection)、E(report-gen) 逐分支审查
  - B：编译通过，安全烟雾测试 7/7 通过
  - E：编译通过，需 rebase → 已完成 rebase 到最新 main
  - D：发现目录嵌套问题 → 修复指南交付 → D 修复后重新检查通过
  - C：在 team/main 上，编译 + test.sh 通过
- **三次集成合并**：
  - 第一次：A 合入（解决 4 个冲突）
  - 第二次：B + E 合入（解决 1 个冲突）
  - 第三次：D 合入（手动提取 D 文件，激活 app.ts 注册）
- **两次集成测试**：
  - 第一次：A+B+C+E 4 模块联通，12 项接口测试通过
  - 第二次：A+B+C+E+D 6 模块联通，8 项接口测试通过
- **全链路端到端测试**：注册→创建赛事→发布→选手报名→审核自动生成 RaceProject→审查预警→提交作品→分配评委→评分→发布奖项→生成报告→公开赛果，17 步全部通过
- **CA 防伪防篡改方案**：补齐 `docs/ary-ca-integration-spec.md` 第 8 节，包含 peerSessionId 认证 + HMAC-SHA256 签名 + nonce 防重放 + 隔离审计 + 服务端校验伪代码
- **P0 前端收口**：Rider Profile 接 API（四卡片数据驱动）、Cooperation 动态赛事数据、GitHub OAuth 真实接入（passport-github2 + .env 管理密钥）、Header 导航高亮

### 文件产出

**新建文件**（8 个）：

| 文件 | 说明 |
|------|------|
| `docs/ary-dev-1-data-model.md` | DEV-1 架构设计 |
| `docs/first-integration-review.md` | 第一次集成审查报告 |
| `docs/first-integration-test-record.md` | 第一次集成测试记录 |
| `docs/second-integration-test-record.md` | 第二次集成测试记录（全套 6 模块） |
| `.agents/skills/hifi-ui-page-workflow/SKILL.md` | 高保真页面工作流 |
| `开发指南.md` | 框架介绍 + 工作流程 + 每人交付物 |
| `server/.env` | GitHub OAuth 密钥配置（.gitignore 排除） |
| 所有 `server/src/`、`client/src/`、`database/migrations/` 源代码文件 | |

**修改文件**（15+）：`PLAN.md`、`STATUS.md`、`docs/ary-ca-integration-spec.md`、`docs/registration-ca-rules-alignment.taskbook.md`、`design-prototype/`（5 文件）、`server/src/app.ts`、`server/src/shared/auth.ts`、`server/src/shared/types.ts`、`.gitignore` 等

### 分支状态

| 分支 | 说明 |
|------|------|
| `main` | 原始骨架（team 远程） |
| `merge-test` | **集成测试分支**，含全部 6 个模块 ✅ |

---

## 二、剩余工作

### 1. DCR 真机联调

- **内容**：在 Windows 上启动 `DCR-Desktop-App/dcr-desktop-shell.exe`，对接 B 的 race-mgmt 模块
- **操作**：①启动 DCR → ②`dcr login` 到 ARY → ③`dcr peer-bind` 注册 CAConnection → ④骑行过程中 push 信号 → ⑤验证 ARY 端签名校验/防重放
- **负责人**：B（race-mgmt 模块作者）

### 2. 彩排与发布（REL-1）

- **内容**：Staging 环境全流程彩排 + 灰度发布 + 生产部署
- **操作**：①部署到 staging → ②全流程演练（登录→创建赛事→报名→CA接入→Live Hall→提交→评审→发榜→报告→大屏）→ ③P0 回归一键跑通 → ④灰度发布 → ⑤正式生产发布
- **负责人**：全体，A 协调

### 3. OPS-1 运维准备

- **内容**：值守表、回滚方案、备份脚本、监控告警
- **负责人**：B + D

### 4. 可选优化

- 前端视觉对标 `design-prototype/index.html`（蓝白竞赛主题、glass-card 等）
- 页面移动端适配
- `server/tests/p0-smoke.sh` 集成回归脚本（D 的 CI 交付物）

---

## 三、如何继续

### 启动项目

```bash
git clone https://github.com/CHEF-Ch/ARY-3.git
cd ARY-3
git checkout merge-test
cd server && npm install && cp .env.example .env  # 或手动创建 .env
npm run dev          # → http://localhost:3000
cd ../client && npm install && npm run dev          # → http://localhost:5173
```

### 验证集成

```bash
# 编译检查
cd server && npx tsc --noEmit
cd client && npx tsc --noEmit

# 全链路回归
# 参考 docs/second-integration-test-record.md 中的 curl 命令
```

### .env 文件内容（server/.env）

```
GITHUB_CLIENT_ID=Ov23liV3UARUzBRDPbU8
GITHUB_CLIENT_SECRET=820b28af8fc2ed82e61e8385287f0a4be7964c5a
SESSION_SECRET=ary-dev-session-change-in-production
```

--

🤖 集成测试与审查由 Claude 完成。剩余工作交付小组执行。
